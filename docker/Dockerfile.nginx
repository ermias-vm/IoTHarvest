# Dockerfile para el servidor Nginx que actúa como reverse proxy
FROM nginx:alpine

# Eliminar la configuración predeterminada
RUN rm /etc/nginx/conf.d/default.conf

# Copiar nuestra configuración personalizada
COPY docker/nginx.conf /etc/nginx/conf.d/

# Crear directorios para logs
RUN mkdir -p /var/log/nginx

# Exponer puertos
EXPOSE 80
# EXPOSE 443  # Descomenta si usas HTTPS

CMD ["nginx", "-g", "daemon off;"]
